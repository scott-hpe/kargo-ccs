apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook
  namespace: ccs-guestbook-mira-us-west-2
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: guestbook
  template:
    metadata:
      labels:
        app: guestbook
    spec:
      containers:
      - env:
        - name: FOO
          value: disable
        image: ghcr.io/scott-hpe/guestbook:v0.0.152
        name: guestbook
        ports:
        - containerPort: 3000
          name: http
