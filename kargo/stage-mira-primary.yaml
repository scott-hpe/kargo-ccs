apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  annotations:
    kargo.akuity.io/color: amber
  name: mira-primary
  namespace: kargo-ccs
spec:
  promotionTemplate:
    spec:
      vars:
        - name: cluster
          value: "mira-${{ sharedConfigMap('cluster-region-state')[ctx.stage] }}"
      steps:
        - task:
            name: promote
  requestedFreight:
    - origin:
        kind: Warehouse
        name: ccs-guestbook
      sources:
        stages:
          - polaris